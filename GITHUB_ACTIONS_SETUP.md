# Настройка GitHub Actions

GitHub Actions workflows созданы и готовы к использованию. Для их активации нужно выполнить один из вариантов ниже.

## ✅ Что уже готово:

1. **build.yml** - автоматическая сборка проекта при каждом push
2. **deploy-pages.yml** - автоматический деплой на GitHub Pages
3. **ci.yml** - проверка сборки при pull requests

## 🔧 Как активировать:

### Вариант 1: Через веб-интерфейс GitHub (рекомендуется)

1. Откройте репозиторий: https://github.com/vostroslava/stalking-copy
2. Перейдите в раздел **Settings** → **Actions** → **General**
3. Убедитесь, что "Allow all actions and reusable workflows" включено
4. Файлы workflow уже созданы локально, их нужно добавить через веб-интерфейс или обновить токен

### Вариант 2: Обновить Personal Access Token

1. Перейдите: https://github.com/settings/tokens
2. Создайте новый токен (или обновите существующий)
3. Включите scope: **`workflow`**
4. Используйте этот токен для аутентификации

### Вариант 3: Добавить файлы вручную через GitHub

Файлы workflow находятся в папке `.github/workflows/`. Вы можете:
- Скопировать их содержимое
- Создать файлы через веб-интерфейс GitHub
- Или использовать GitHub CLI

## 📋 Содержимое workflow файлов:

### `.github/workflows/build.yml`
Автоматически собирает проект при каждом push в main

### `.github/workflows/deploy-pages.yml`
Автоматически деплоит собранный сайт на GitHub Pages

### `.github/workflows/ci.yml`
Проверяет, что проект собирается без ошибок

## 🚀 После активации:

1. Workflows будут автоматически запускаться при каждом push
2. Вы сможете видеть статус сборки на вкладке **Actions**
3. После первого успешного деплоя сайт будет доступен на GitHub Pages

## 📝 Настройка GitHub Pages:

1. Перейдите в **Settings** → **Pages**
2. В разделе "Source" выберите **GitHub Actions**
3. После первого деплоя сайт будет доступен по адресу:
   `https://vostroslava.github.io/stalking-copy/`

## 🔍 Проверка статуса:

После push вы можете проверить статус workflows:
- Вкладка **Actions** в репозитории
- Зеленый значок ✅ означает успешную сборку
- Красный ❌ означает ошибку (можно посмотреть логи)

