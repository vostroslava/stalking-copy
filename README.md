# Сталкер-Медиа — Модернизированный сайт

Современная версия сайта stalking.by, созданная с использованием Vite.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev:vite
```

Сайт будет доступен по адресу `http://localhost:5173`

### Сборка для продакшена
```bash
npm run build:vite
```

Собранные файлы будут в папке `dist/`

### Просмотр собранной версии
```bash
npm run preview:vite
```

## 📁 Структура проекта

```
stalking-copy/
├── src/                    # Исходники нового сайта
│   ├── index.html          # Главная страница
│   ├── catalog.html        # Каталог программ
│   ├── styles/             # CSS стили
│   ├── js/                 # JavaScript
│   ├── partials/          # Частичные шаблоны (header, footer)
│   ├── programs/          # Детальные страницы программ
│   └── articles/           # Статьи
├── stalking.by/           # Оригинальный сайт (для справки)
├── dist/                  # Собранные файлы (генерируется)
└── vite.config.js         # Конфигурация Vite
```

## 🔧 Технологии

- **Vite** — сборщик и dev-сервер
- **Vanilla JavaScript** — без фреймворков
- **CSS Variables** — для темизации
- **Responsive Design** — адаптивная верстка

## 📝 Работа с Git

### Подключение к GitHub

1. Создайте новый репозиторий на GitHub
2. Добавьте remote:
   ```bash
   git remote add origin https://github.com/ваш-username/название-репозитория.git
   ```
3. Отправьте код:
   ```bash
   git branch -M main
   git push -u origin main
   ```

### Основные команды

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения
git pull
```

## 🎨 Особенности

- Современный дизайн с градиентами и анимациями
- Адаптивная верстка для всех устройств
- Динамическая загрузка header и footer
- SEO-оптимизация с мета-тегами и JSON-LD
- Плавная прокрутка и интерактивные элементы

## 📄 Лицензия

Все права защищены © Сталкер-Медиа

